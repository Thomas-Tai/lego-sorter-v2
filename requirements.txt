# Core application dependencies
pandas>=2.0.0
PyYAML>=6.0
pydantic>=2.0.0

# Vision - use headless for Raspberry Pi (no GUI dependencies)
opencv-python-headless>=4.5.0

# Hardware (Pi only - will fail gracefully on non-Pi)
gpiozero>=2.0
rpi-lgpio>=0.6  # Required for Pi 5 compatibility
# RPi.GPIO>=0.7.0  # Legacy, use lgpio instead for Pi 5

# TensorFlow Lite runtime will be added later for the Pi
